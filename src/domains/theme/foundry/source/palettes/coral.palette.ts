/**
 * Coral — free accent. Warm salmon, distinct from danger red.
 */
import type { PaletteSource } from "../../foundry.types";

export const coralPalette: PaletteSource = {
  name: "coral",
  kind: "accent",
  anchor: { l: 0.68, c: 0.20, h: 25 },
  curve: "brand",
  chroma: "linear-to-500",
};
