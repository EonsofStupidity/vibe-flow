/** Sage — desaturated soft green accent. */
import type { PaletteSource } from "../../foundry.types";
export const sagePalette: PaletteSource = {
  name: "sage", kind: "accent",
  anchor: { l: 0.72, c: 0.06, h: 150 },
  curve: "muted", chroma: "soft",
};
