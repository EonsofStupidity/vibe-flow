/**
 * Lime — free accent. Annotation / ink accent color.
 * Derived from ViziWizi `--vf-matrix` family.
 */
import type { PaletteSource } from "../../foundry.types";

export const limePalette: PaletteSource = {
  name: "lime",
  kind: "accent",
  anchor: { l: 0.78, c: 0.20, h: 128 },
  curve: "brand",
  chroma: "linear-to-500",
};
