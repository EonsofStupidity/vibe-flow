/** Midnight — deep blue surface neutral. */
import type { PaletteSource } from "../../foundry.types";
export const midnightPalette: PaletteSource = {
  name: "midnight", kind: "surface",
  anchor: { l: 0.20, c: 0.04, h: 260 },
  curve: "low-key", chroma: "flat",
};
